[project]
name = "simil"
version = "1.0.0"
description = "Find similarity (rigid body) body transformation parameters."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.4",
]
